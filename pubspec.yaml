name: citas_medicas
description: "Aplicacion de citas medicas con Flutter y SQLite"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path: ^1.8.3
  sqflite_common_ffi: 2.3.3
  sqlite3: 2.4.7
  sqlite3_flutter_libs: ^0.5.24
  sqflite_common_ffi_web: ^0.4.5+3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
